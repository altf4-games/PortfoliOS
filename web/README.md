# PortfoliOS (web app)

See the [repo root README](../README.md) for what this is and how it's structured.

## Local development

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000). Copy `.env.example` to `.env.local` first —
see the root README for what each variable does.
